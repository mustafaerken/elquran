import Navigation from './Navigation';
import List from './List';

const Main=()=>{

  return(
    <div style={{display:'block',width:'100%',height:'100%',paddingBottom:100}}>
      <Navigation/>
      <List/>
    </div>
  );
}

export default Main;
